[project]
name = "spy_be_glair"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = ["spylang"]

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[tool.setuptools]
packages = ["spy_be_glair"]
package-dir = {"spy_be_glair" = "."}
